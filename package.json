{
  "name": "pbg",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "dev:server": "npm run dev --workspace=apps/server",
    "dev:mp": "npm run dev:server & npm run dev",
    "build": "npm run build --workspace=apps/web && rm -rf dist && cp -r apps/web/dist dist",
    "build:server": "npm run build --workspace=apps/server"
  }
}
